# UTAG=value DPORT=value docker-compose build
# docker-compose up

version: "3.7"

services:
  drill:
    build: ./
    image: wumingzhilian/pwntools-level00_arm:v1
    container_name: pwntools-level00_arm
    ports:
      - 56105:2462
    privileged: false
