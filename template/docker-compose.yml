# UTAG=value DPORT=value docker-compose build
# docker-compose up

version: "3.7"

services:
  drill:
    build: ./
    image: wumingzhilian/{name}:v1
    container_name: {name}
    ports:
      - 56105:{port}
    privileged: false
